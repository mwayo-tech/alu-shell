#!/usr/bin/env bash
# Groups visitors by IP and HTTP status code, counts occurrences, and sorts descending

awk '
{
    key = $1 " " $9
    count[key]++
}
END {
    for (k in count)
        print count[k], k
}' apache-access.log | sort -rn
